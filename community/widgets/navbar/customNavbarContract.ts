import { Contract } from "@paperbits/common";

/**
 * Data contact (data layer) that defines how widget configuration gets persisted.
 */
export interface CustomNavbarContract extends Contract {
    // property: string;
}
